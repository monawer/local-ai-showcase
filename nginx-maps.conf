# ترويسة Connection مشروطة بوجود Upgrade — يمنع إرسال Connection: upgrade
# على الطلبات العادية مما يسبب فشل n8n/WebUI خلف Traefik.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
